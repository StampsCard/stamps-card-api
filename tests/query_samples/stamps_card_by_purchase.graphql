# Query

query getSampsCardByPurchase($purchaseId: ID!) {
	stampsCardByPurchase(purchaseId: $purchaseId) {
    stampsCard {
      id
      stamp_price
    }
    spent
    amount
  }
}

# Variable
{
  "purchaseId": ""
}